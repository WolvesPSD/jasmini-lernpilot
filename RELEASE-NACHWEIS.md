# RELEASE-NACHWEIS – nicht veröffentlicht

Stand: 21. September 2026

## Paketinhalt

- `index.html`, `styles.css`, `app.js`: vollständige statische Browser-App mit relativen Pfaden.
- `_headers`: datensparsame Sicherheitsvorgaben für Hosts, die diese Datei unterstützen.
- `.nojekyll`: unveränderte statische Auslieferung bei GitHub Pages.
- `README.md`, `DEPLOYMENT-OPTIONEN.md`: Bedien- und Bereitstellungsgrenzen.

Das Paket startet mit `Pilotprofil (leer)` und ohne Beobachtungen. Es enthält keine Namen von Kindern, keine lokalen JSON-Exporte und keine vorbereiteten Lernstände.

## Lokale Prüfung

- Temporäre statische HTTP-Vorschau: `index.html`, `styles.css` und `app.js` laden mit relativen Pfaden.
- Startansicht: generisches Profil, keine vorbereiteten Beobachtungen und keine sichtbaren Projektnamen von Personen.
- Ablauf: Wochenblattvorschau mit sechs vorhandenen Aufgaben; Kinderblatt ohne Lösung oder Antwort.
- Geräteansicht: bei 390 px keine horizontale Seitenüberbreite; Druckschaltflächen sichtbar.
- Browserkonsole: keine Fehler.

## Veröffentlichung

GitHub Pages ist als kostenloser statischer Weg dokumentiert. Die GitHub-Startseite war erreichbar, zeigte jedoch **Sign in**. Deshalb wurden kein Konto, kein Repository, kein Upload, keine Pages-Einstellung und keine Freigabe erstellt.

Der einzige manuelle Schritt ist eine vorhandene GitHub-Anmeldung im Browser. Danach darf ausschliesslich dieser Ordner in ein neues öffentliches Repository hochgeladen und GitHub Pages aus dessen Hauptbranch aktiviert werden. Vorher keine JSON-Exportdatei oder lokale Projektkopie auswählen.
